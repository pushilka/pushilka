# Pushilka

## Installation

```
npm install pushilka
```

## Usage

```
var pushilka = new Pushilka({
    source: "<source token>",
    serviceWorker: "<path to dist/serviceWorker.js>"
})
pushilka.run()
```